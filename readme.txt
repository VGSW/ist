run via bottle
    # pacman -S python-bottle
    python3 index.py

commandline invocation
    python3 main.py --sort=desc --sort_col=country > output.html

get usage
    python3 main.py --help

the files data.csv and data.sqlite3 are expected in the same directory as main.py
